apiVersion: v1
kind: Service
metadata:
  name: community-backend
  namespace: community
  labels:
    app: community-backend
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: community-backend
  ports:
    - name: http
      port: 80
      targetPort: http
